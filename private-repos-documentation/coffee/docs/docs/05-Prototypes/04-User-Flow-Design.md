# 用戶流程設計 (User Flow Design)

## 1. 流程設計概述

### 1.1 設計原則
- **簡化步驟**: 最小化用戶完成任務所需的步驟
- **清晰導航**: 用戶始終知道自己在哪裡，下一步該做什麼
- **錯誤處理**: 提供清晰的錯誤訊息和恢復路徑
- **一致性**: 相似的任務使用相似的流程模式

### 1.2 流程分類
- **CRM 核心流程**: 店員日常操作流程
- **ERP 管理流程**: 總部管理決策流程
- **會員服務流程**: 客戶體驗相關流程
- **系統維護流程**: 技術和管理維護流程

---

## 2. CRM 系統用戶流程

### 2.1 店員登入流程
```mermaid
graph TD
    A[開啟應用程式] --> B[顯示登入頁面]
    B --> C[輸入員工編號]
    C --> D[輸入密碼]
    D --> E{驗證身份}
    E -->|成功| F[記錄登入時間]
    E -->|失敗| G[顯示錯誤訊息]
    F --> H[載入用戶權限]
    H --> I[顯示 POS 主頁面]
    G --> J{錯誤次數}
    J -->|<3 次| C
    J -->|≥3 次| K[帳號暫時鎖定]
    K --> L[通知管理員]
```

**關鍵決策點:**
- **身份驗證**: 3 次失敗後鎖定 30 分鐘
- **權限載入**: 根據角色載入對應功能模組
- **自動登出**: 超過 8 小時無操作自動登出

### 2.2 POS 點餐完整流程
```mermaid
graph TD
    A[進入 POS 系統] --> B[選擇商品分類]
    B --> C[瀏覽商品清單]
    C --> D[點選商品]
    D --> E[開啟商品選項對話框]
    E --> F[選擇尺寸/溫度/配料]
    F --> G[設定數量]
    G --> H[確認加入購物車]
    H --> I[商品加入購物車]
    I --> J{繼續選購?}
    J -->|是| C
    J -->|否| K[檢查購物車內容]
    K --> L{會員客戶?}
    L -->|是| M[會員身份驗證]
    L -->|否| N[進入結帳流程]
    M --> O[套用會員折扣]
    O --> P{使用點數?}
    P -->|是| Q[選擇點數使用方式]
    P -->|否| N
    Q --> R[計算點數折抵]
    R --> N[進入結帳流程]
    N --> S[選擇付款方式]
    S --> T{付款方式}
    T -->|現金| U[輸入收款金額]
    T -->|卡片| V[刷卡處理]
    T -->|行動支付| W[掃描 QR Code]
    U --> X[計算找零]
    V --> Y[確認交易]
    W --> Y
    X --> Y
    Y --> Z[列印收據]
    Z --> AA[訂單完成]
    AA --> BB[清空購物車]
```

### 2.3 會員查詢與服務流程
```mermaid
graph TD
    A[點擊會員查詢] --> B[開啟查詢對話框]
    B --> C[輸入手機號碼]
    C --> D[點擊查詢按鈕]
    D --> E{會員存在?}
    E -->|是| F[顯示會員資訊]
    E -->|否| G[顯示找不到會員]
    F --> H[顯示會員等級]
    H --> I[顯示可用點數]
    I --> J{操作選擇}
    J -->|套用會員| K[將會員資訊關聯到訂單]
    J -->|使用點數| L[開啟點數使用對話框]
    J -->|查看詳情| M[跳轉到會員詳情頁]
    G --> N{創建新會員?}
    N -->|是| O[開啟會員註冊表單]
    N -->|否| P[關閉對話框]
    L --> Q[選擇點數使用數量]
    Q --> R[確認點數折抵]
    R --> S[更新訂單總額]
    O --> T[填寫會員基本資料]
    T --> U[儲存新會員]
    U --> V[自動套用到當前訂單]
```

### 2.4 客戶管理流程
```mermaid
graph TD
    A[進入客戶管理] --> B[載入客戶清單]
    B --> C{操作選擇}
    C -->|新增客戶| D[開啟新增客戶表單]
    C -->|搜尋客戶| E[輸入搜尋條件]
    C -->|查看客戶| F[點擊客戶列表項]
    D --> G[填寫客戶基本資料]
    G --> H[設定會員等級]
    H --> I[儲存客戶資料]
    I --> J[返回客戶清單]
    E --> K[顯示搜尋結果]
    K --> F
    F --> L[載入客戶詳情]
    L --> M[顯示客戶資訊]
    M --> N[顯示消費記錄]
    N --> O[顯示點數記錄]
    O --> P{編輯客戶?}
    P -->|是| Q[開啟編輯表單]
    P -->|否| R[返回清單]
    Q --> S[修改客戶資料]
    S --> T[儲存變更]
    T --> R
```

---

## 3. ERP 系統用戶流程

### 3.1 管理員登入與儀表板流程
```mermaid
graph TD
    A[開啟 ERP 系統] --> B[顯示登入頁面]
    B --> C[輸入管理員帳號]
    C --> D[輸入密碼]
    D --> E[輸入雙因子驗證碼]
    E --> F{身份驗證}
    F -->|成功| G[載入管理權限]
    F -->|失敗| H[顯示錯誤訊息]
    G --> I[記錄登入日誌]
    I --> J[載入儀表板數據]
    J --> K[顯示 KPI 指標]
    K --> L[載入圖表數據]
    L --> M[顯示營運總覽]
    H --> N{重試次數}
    N -->|<5 次| C
    N -->|≥5 次| O[帳號鎖定]
    O --> P[發送安全警告]
```

### 3.2 門店管理流程
```mermaid
graph TD
    A[進入門店管理] --> B[載入門店清單]
    B --> C{操作選擇}
    C -->|新增門店| D[開啟新增門店表單]
    C -->|查看門店| E[點擊門店卡片]
    C -->|編輯門店| F[點擊編輯按鈕]
    D --> G[填寫門店基本資訊]
    G --> H[設定營業時間]
    H --> I[配置員工權限]
    I --> J[設定庫存參數]
    J --> K[儲存門店資料]
    K --> L[生成門店編號]
    L --> M[發送設定通知]
    E --> N[載入門店詳情]
    N --> O[顯示基本資訊]
    O --> P[載入業績數據]
    P --> Q[顯示員工清單]
    Q --> R[顯示庫存狀態]
    F --> S[載入編輯表單]
    S --> T[修改門店資訊]
    T --> U[驗證資料完整性]
    U --> V[儲存變更]
    V --> W[通知相關人員]
```

### 3.3 庫存管理流程
```mermaid
graph TD
    A[進入庫存管理] --> B[載入庫存總覽]
    B --> C[顯示庫存警告]
    C --> D{操作選擇}
    D -->|新增商品| E[開啟商品新增表單]
    D -->|進貨作業| F[進入進貨管理]
    D -->|調整庫存| G[選擇要調整的商品]
    D -->|查看報表| H[進入庫存報表]
    E --> I[填寫商品基本資訊]
    I --> J[設定庫存參數]
    J --> K[配置供應商資訊]
    K --> L[設定安全庫存]
    L --> M[儲存商品資料]
    F --> N[創建進貨單]
    N --> O[選擇供應商]
    O --> P[添加進貨商品]
    P --> Q[設定數量和價格]
    Q --> R[計算進貨總額]
    R --> S[提交審核]
    S --> T[等待主管核准]
    T --> U{審核結果}
    U -->|通過| V[發送訂單給供應商]
    U -->|拒絕| W[通知修改原因]
    G --> X[輸入調整數量]
    X --> Y[選擇調整原因]
    Y --> Z[確認庫存調整]
    Z --> AA[記錄調整日誌]
```

### 3.4 財務報表生成流程
```mermaid
graph TD
    A[進入財務報表] --> B[選擇報表類型]
    B --> C{報表類型}
    C -->|損益表| D[設定查詢期間]
    C -->|現金流量表| E[選擇現金流分類]
    C -->|資產負債表| F[設定資產計算基準]
    D --> G[載入收入數據]
    G --> H[載入成本數據]
    H --> I[計算毛利潤]
    I --> J[載入營運費用]
    J --> K[計算淨利潤]
    K --> L[生成損益表]
    E --> M[載入現金收入]
    M --> N[載入現金支出]
    N --> O[計算現金流量]
    O --> P[生成現金流量表]
    F --> Q[載入資產明細]
    Q --> R[載入負債明細]
    R --> S[計算股東權益]
    S --> T[生成資產負債表]
    L --> U[顯示報表]
    P --> U
    T --> U
    U --> V{匯出報表?}
    V -->|是| W[選擇匯出格式]
    V -->|否| X[結束]
    W --> Y[生成檔案]
    Y --> Z[下載報表]
```

---

## 4. 會員服務流程

### 4.1 會員註冊流程
```mermaid
graph TD
    A[客戶首次消費] --> B{提供手機號碼?}
    B -->|是| C[檢查是否已是會員]
    B -->|否| D[完成非會員交易]
    C --> E{已是會員?}
    E -->|是| F[套用會員優惠]
    E -->|否| G[詢問是否註冊會員]
    G --> H{願意註冊?}
    H -->|是| I[開啟會員註冊表單]
    H -->|否| D
    I --> J[填寫基本資料]
    J --> K[選擇接收行銷訊息]
    K --> L[同意會員條款]
    L --> M[送出註冊申請]
    M --> N[系統驗證資料]
    N --> O{資料正確?}
    O -->|是| P[建立會員帳戶]
    O -->|否| Q[顯示錯誤訊息]
    P --> R[發送歡迎簡訊]
    R --> S[給予註冊點數]
    S --> T[套用到當前訂單]
    Q --> U[修正資料]
    U --> M
```

### 4.2 點數累積與使用流程
```mermaid
graph TD
    A[會員完成消費] --> B[計算消費金額]
    B --> C[查詢會員等級]
    C --> D[計算點數回饋率]
    D --> E[計算可獲得點數]
    E --> F[累積到會員帳戶]
    F --> G[檢查是否升級]
    G --> H{符合升級條件?}
    H -->|是| I[升級會員等級]
    H -->|否| J[維持現有等級]
    I --> K[發送升級通知]
    K --> L[更新會員卡]
    J --> M[交易完成]
    L --> M
    
    N[會員想使用點數] --> O[查詢可用點數]
    O --> P[選擇使用點數數量]
    P --> Q[計算折抵金額]
    Q --> R{點數足夠?}
    R -->|是| S[扣除點數]
    R -->|否| T[提示點數不足]
    S --> U[更新訂單金額]
    U --> V[記錄點數使用記錄]
    T --> W[建議部分使用]
    W --> X{接受建議?}
    X -->|是| Y[使用部分點數]
    X -->|否| Z[不使用點數]
    Y --> S
```

### 4.3 會員等級升級流程
```mermaid
graph TD
    A[系統定期檢查會員] --> B[計算會員累積消費]
    B --> C[計算會員消費頻次]
    C --> D[評估會員活躍度]
    D --> E{符合升級條件?}
    E -->|是| F[準備升級作業]
    E -->|否| G[維持現有等級]
    F --> H[更新會員等級]
    H --> I[計算新等級優惠]
    I --> J[發送升級通知]
    J --> K[贈送升級禮品]
    K --> L[更新會員權益]
    L --> M[記錄升級日誌]
    
    N[會員等級降級檢查] --> O[計算近期消費]
    O --> P[檢查消費活躍度]
    P --> Q{低於保級標準?}
    Q -->|是| R[發送保級提醒]
    Q -->|否| S[維持等級]
    R --> T[設定保級期限]
    T --> U[等待客戶回應]
    U --> V{期限內達標?}
    V -->|是| S
    V -->|否| W[降級會員等級]
    W --> X[發送降級通知]
    X --> Y[調整會員權益]
```

---

## 5. 系統維護流程

### 5.1 日常營運開店流程
```mermaid
graph TD
    A[店員到達門店] --> B[使用門店鑰匙開門]
    B --> C[開啟店內照明和設備]
    C --> D[登入 POS 系統]
    D --> E[執行開店檢查清單]
    E --> F[檢查設備運行狀態]
    F --> G[確認庫存狀況]
    G --> H[檢查昨日營收結算]
    H --> I[準備今日所需物料]
    I --> J[設定門店營業狀態為"營業中"]
    J --> K[系統自動通知總部開店]
    K --> L[開始接受訂單]
    
    M[準備打烊] --> N[停止接受新訂單]
    N --> O[完成最後訂單]
    O --> P[執行日終結算]
    P --> Q[統計今日營收]
    Q --> R[檢查庫存消耗]
    R --> S[清潔設備和環境]
    S --> T[關閉設備電源]
    T --> U[鎖定 POS 系統]
    U --> V[設定門店狀態為"休息中"]
    V --> W[上傳營業數據到總部]
    W --> X[鎖門離店]
```

### 5.2 庫存盤點流程
```mermaid
graph TD
    A[啟動盤點作業] --> B[選擇盤點範圍]
    B --> C{盤點類型}
    C -->|全盤| D[列印全部商品清單]
    C -->|抽盤| E[系統隨機選擇商品]
    C -->|循環盤| F[按排程選擇商品]
    D --> G[分配盤點人員]
    E --> G
    F --> G
    G --> H[開始實地盤點]
    H --> I[掃描商品條碼]
    I --> J[記錄實際數量]
    J --> K[系統對比帳面數量]
    K --> L{數量一致?}
    L -->|是| M[標記盤點完成]
    L -->|否| N[標記為差異項目]
    M --> O{還有商品?}
    N --> P[記錄差異原因]
    P --> O
    O -->|是| I
    O -->|否| Q[產生盤點報表]
    Q --> R[主管審核差異]
    R --> S{差異原因合理?}
    S -->|是| T[調整庫存帳面數量]
    S -->|否| U[要求重新盤點]
    T --> V[記錄盤點結果]
    U --> I
    V --> W[盤點作業完成]
```

### 5.3 系統備份與維護流程
```mermaid
graph TD
    A[系統自動觸發備份] --> B[檢查系統負載]
    B --> C{負載是否過高?}
    C -->|是| D[延遲 30 分鐘再執行]
    C -->|否| E[開始資料備份]
    E --> F[備份交易數據]
    F --> G[備份客戶數據]
    G --> H[備份庫存數據]
    H --> I[備份系統設定]
    I --> J[壓縮備份檔案]
    J --> K[上傳到雲端儲存]
    K --> L{備份成功?}
    L -->|是| M[記錄備份成功]
    L -->|否| N[記錄備份失敗]
    M --> O[清理過期備份]
    N --> P[發送警告通知]
    O --> Q[備份作業完成]
    P --> R[技術人員介入]
    
    S[系統維護檢查] --> T[檢查系統效能]
    T --> U[檢查磁碟空間]
    U --> V[檢查記憶體使用]
    V --> W[檢查網路連線]
    W --> X{發現問題?}
    X -->|是| Y[記錄問題詳情]
    X -->|否| Z[系統狀態正常]
    Y --> AA[通知技術團隊]
    AA --> BB[排程維護作業]
```

---

## 6. 錯誤處理流程

### 6.1 網路連線中斷處理
```mermaid
graph TD
    A[檢測到網路中斷] --> B[暫停線上作業]
    B --> C[切換到離線模式]
    C --> D[提示用戶網路問題]
    D --> E[暫存交易數據到本地]
    E --> F[定時嘗試重新連線]
    F --> G{連線恢復?}
    G -->|是| H[同步暫存數據]
    G -->|否| I[繼續離線模式]
    H --> J[驗證數據完整性]
    J --> K[上傳到伺服器]
    K --> L{同步成功?}
    L -->|是| M[恢復線上模式]
    L -->|否| N[記錄同步失敗]
    M --> O[通知用戶恢復正常]
    N --> P[等待下次同步]
    I --> Q{超過 24 小時?}
    Q -->|是| R[聯繫技術支援]
    Q -->|否| F
```

### 6.2 付款失敗處理流程
```mermaid
graph TD
    A[付款處理失敗] --> B{失敗原因分析}
    B -->|餘額不足| C[提示餘額不足]
    B -->|卡片問題| D[提示更換付款方式]
    B -->|系統錯誤| E[提示稍後重試]
    B -->|網路問題| F[提示網路異常]
    C --> G[建議其他付款方式]
    D --> G
    E --> H[記錄錯誤日誌]
    F --> I[嘗試重新連線]
    G --> J[客戶選擇新的付款方式]
    J --> K[重新處理付款]
    H --> L[技術人員檢查]
    I --> M{連線恢復?}
    M -->|是| K
    M -->|否| N[建議使用現金]
    K --> O{付款成功?}
    O -->|是| P[完成交易]
    O -->|否| Q[再次失敗處理]
    Q --> R[主管介入]
    R --> S[人工處理]
```

---

## 7. 流程優化建議

### 7.1 效率提升要點
1. **減少點擊次數**: 常用功能一鍵直達
2. **預填默認值**: 根據歷史行為智慧預填
3. **批量操作**: 支援多選批量處理
4. **快捷鍵**: 為熟練用戶提供鍵盤快捷鍵

### 7.2 用戶體驗優化
1. **進度提示**: 長時間操作顯示進度條
2. **操作確認**: 重要操作需要二次確認
3. **自動儲存**: 防止數據意外丟失
4. **智慧提示**: 根據上下文提供操作建議

### 7.3 錯誤預防
1. **資料驗證**: 即時驗證用戶輸入
2. **操作引導**: 新用戶提供操作指引
3. **狀態提示**: 清楚顯示當前系統狀態
4. **回復機制**: 提供操作撤銷功能

---

**版本**: v1.0  
**建立日期**: 2025-01-16  
**最後更新**: 2025-01-16  
**負責人**: UI/UX 設計團隊

**流程圖說明**:
- 所有流程圖使用 Mermaid 語法撰寫
- 決策點用菱形表示，操作用矩形表示
- 不同顏色代表不同的流程狀態
- 關鍵流程節點有詳細的業務邏輯說明